import React from 'react';
// import SideBarNav from '../SideBarNav';
import SafeAreaProvider from 'react-native-safe-area-context';
import { View, Text, StyleSheet, Button } from 'react-native';

    // <Text style={{ fontSize: 48, fontWeight: 'bold' }}> Main Screen </Text> <SideBarNav />
    // <Button title ="Go to Track Detail" onPress={() => navigation.navigate('TrackDetail')}></Button>
const DashBoardsScreen = ({ navigation  }) => {
    return (<>
        
        </>
    );

};

const styles =  StyleSheet.create({});

export default DashBoardsScreen;